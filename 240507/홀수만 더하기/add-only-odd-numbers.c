#include <stdio.h>

int main() {
    int n, N, i;
    int sum_val=0;
    scanf("%d", &n);
    for(i=1; i<=n; i++) {
        scanf("%d/n", &n);
    }
    for(i=1; i<=N; i++) {
        sum_val+= N;
    }
    printf("%d", sum_val);
    return 0;
}